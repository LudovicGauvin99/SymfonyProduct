{% extends 'base.html.twig' %}

{% block title %}
	Search Product
{% endblock %}

{% block body %}

	<h2>Résultat de la recherche</h2>
	<ul>
		{% for product in products %}
			<li>
                <h3>{{product.name}}</h3>
				<img style ="width: 20%;" src="{{ asset('img/' ~ product.image ) }}" alt="{{product.image}}">
				<div>
					<p>Le prix est de : {{product.prix|format_currency('EUR', locale='fr')}}</p>
				</div>
			</li>
		{% endfor %}
	</ul>

{% endblock %}